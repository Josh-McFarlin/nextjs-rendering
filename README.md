# Next.js Rendering

An example application showing different rendering methods with Next.js

## Running

### Development

1. Run `npm install`
2. Run `npm run dev`

### Production

1. Run `npm install`
2. Run `npm run start`
